L = [input()]
temp = none
def Bb(L):
    for 0 in len(L) - 1
    if float(L[i]) > float(L[i + 1]):
        return float(L[i]) =temp
        float(L(i + 1)) = float(L[i])
        float(L[i] = float(L[i + 1])


print('请输入一串数字：')


